const $=s=>document.querySelector(s);const input=$('#input'),result=$('#result'),notice=$('#notice');if(input){input.addEventListener('input',()=>$('#count').textContent=`${input.value.length}/5000`);$('#translate').onclick=async()=>{notice.textContent='翻译中…';const fd=new FormData();fd.append('text',input.value);fd.append('source',$('#source').value);fd.append('target',$('#target').value);try{const r=await fetch('api/translate.php',{method:'POST',body:fd});const j=await r.json();if(!j.ok){notice.textContent=j.message;return}result.textContent=j.translation;notice.textContent=j.mode==='api'?'已使用翻译 API':'当前为演示模式，请在 config/config.php 配置 API';}catch(e){notice.textContent='网络错误，请稍后再试'}};$('#copy').onclick=()=>navigator.clipboard?.writeText(result.textContent);$('#speak').onclick=()=>speechSynthesis.speak(new SpeechSynthesisUtterance(result.textContent));$('#swap').onclick=()=>{let a=$('#source').value,b=$('#target').value;if(a==='auto')a='en';$('#source').value=b;$('#target').value=a};$('#voice').onclick=()=>{const R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R){notice.textContent='当前浏览器不支持语音输入';return}const r=new R();r.lang='zh-CN';r.onresult=e=>input.value=e.results[0][0].transcript;r.start()}}
